# 医学专用环境说明

本项目优先使用医学影像专用 conda 环境：

```text
fracmed
```

解释器路径：

```text
D:/python/anaconda/envs/fracmed/python.exe
```

该环境从 `th123` 克隆而来，然后额外安装了医学影像相关依赖，避免污染通用环境。

## 1. 创建方式

由于 conda 从 defaults 下载时遇到 SSL 问题，本环境使用本地已有 `th123` 克隆创建：

```powershell
D:/python/anaconda/Scripts/conda.exe create -n fracmed --clone th123 -y
```

## 2. 已安装的医学影像包

额外安装命令：

```powershell
D:/python/anaconda/envs/fracmed/python.exe -m pip install totalsegmentator SimpleITK nibabel plotly
```

关键包包括：

- `TotalSegmentator 2.14.0`
- `SimpleITK 2.5.5`
- `nibabel 5.4.2`
- `plotly 6.8.0`
- `nnunetv2 2.8.0`
- `dicom2nifti 2.6.2`
- `python-gdcm 3.2.6`
- `nnInteractive 2.3.3`
- `torch 2.7.1+cu118`
- `gradio 6.9.0`
- `pydicom 3.0.2`

`pip check` 当前结果：

```text
No broken requirements found.
```

## 3. Demo 启动方式

推荐：

```powershell
.\run_demo_fracmed.ps1
```

或直接：

```powershell
D:/python/anaconda/envs/fracmed/python.exe app.py
```

## 4. 使用原则

- 当前项目后续医学影像模型实验优先使用 `fracmed`；
- 不再向 `th123` 安装 TotalSegmentator、MONAI、nnU-Net 等医学专用大包；
- 如果只是普通 Python/文档任务，可以不启动环境；
- 如果后续接入 TotalSegmentator 或其它医学模型，默认使用 `fracmed`。

## 5. 注意事项

安装时 pip 提示若干命令行脚本不在 PATH 中，例如：

```text
TotalSegmentator.exe
nnUNetv2_predict.exe
```

这不影响项目使用。需要调用时应使用绝对路径，或者通过：

```powershell
D:/python/anaconda/envs/fracmed/python.exe -m <module>
```

后续如果需要直接在终端中使用这些命令，可以先激活环境：

```powershell
D:/python/anaconda/Scripts/conda.exe activate fracmed
```

## 6. TotalSegmentator 探针脚本

项目中预留了一个独立探针脚本，用于在接入页面前测试 TotalSegmentator：

```powershell
D:/python/anaconda/envs/fracmed/python.exe scripts/probe_totalseg.py --case 1
```

默认参数：

```text
task = appendicular_bones
quality = fastest
device = gpu
input_mode = nifti
```

输出目录：

```text
outputs/totalseg_probe/
```

该目录属于运行产物，不提交到 git。

### 6.1 Windows / scikit-image IO 补丁

当前机器上 `from skimage import io` 会触发 Windows 原生异常，并导致 `nnunetv2` 的 image reader registry 导入失败。TotalSegmentator 的 NIfTI 推理并不需要这个 natural image reader，但 nnU-Net 顶层会导入它。

因此 `fracmed` 环境中对以下文件做了本地补丁：

```text
D:/python/anaconda/envs/fracmed/Lib/site-packages/nnunetv2/imageio/natural_image_reader_writer.py
```

补丁内容：

- 移除顶层 `from skimage import io`；
- 改为在读写 PNG/TIFF 时懒加载 `imageio.v3`。

如后续重装 `nnunetv2` 导致补丁丢失，可重新执行：

```powershell
D:/python/anaconda/envs/fracmed/python.exe scripts/patch_nnunet_skimage_io.py
```

验证：

```powershell
D:/python/anaconda/envs/fracmed/python.exe -c "import nnunetv2.inference.predict_from_raw_data; print('ok')"
```

### 6.2 License 限制

`appendicular_bones` 任务最贴近手腕、足部、踝、肘等四肢骨结构，但 TotalSegmentator 当前提示该任务不是开放任务，需要 license。

因此在没有 license 前：

- 不能直接使用 `appendicular_bones` 作为页面中的骨结构专家；
- 可以测试开放的 `total` 任务，但它只覆盖部分大骨，如 humerus、femur、clavicula、scapula 等；
- 对腕骨、跗骨、掌骨、跖骨等小骨，开放 `total` 任务并不够用。

### 6.3 当前探针结果

已完成的验证：

- `fracmed` 环境可以导入 `TotalSegmentator`、`nnunetv2`、`SimpleITK`、`nibabel`；
- `pip check` 无 broken requirements；
- PyTorch CUDA 可用，设备为 RTX 4060 Laptop GPU；
- SimpleITK 可以读取当前 DICOM series；
- `scripts/probe_totalseg.py` 可以将病例 DICOM 导出为 NIfTI；
- 环境补丁后，`nnunetv2.inference.predict_from_raw_data` 可以正常导入。

遇到的问题：

1. `appendicular_bones` 任务会明确提示需要 license，当前无法作为免费外部专家直接接入。
2. 开放 `total` 任务可以下载权重，但实际推理阶段仍以 Windows 原生退出码 `-1066598273` 退出，没有 Python traceback。

因此当前状态是：

- TotalSegmentator 依赖环境已基本准备好；
- DICOM -> NIfTI 前处理链路已打通；
- 但 TotalSegmentator 推理还不能稳定完成；
- 在解决推理崩溃和 license 问题前，不应接入主 demo 页面。

后续排查方向：

- 尝试在 Linux/WSL2 环境运行 TotalSegmentator；
- 尝试使用官方推荐环境或 Docker；
- 尝试不同 `nnunetv2` / `scikit-image` / `torch` 组合；
- 申请/配置 `appendicular_bones` academic license；
- 如果只需要骨结构参考，可优先继续增强当前 HU 阈值骨结构分割，而不是依赖 TotalSegmentator。

## 7. nnInteractive 探针脚本

项目中新增了 nnInteractive 独立探针脚本，用于测试交互式 3D 医学图像分割方向：

```powershell
D:/python/anaconda/envs/fracmed/python.exe scripts/probe_nninteractive.py --case 1
```

该脚本默认会：

- 从 Hugging Face `nnInteractive/nnInteractive` 下载或复用 `nnInteractive_v1.0` 权重；
- 将当前 DICOM case 导出为 NIfTI；
- 在 CT 骨阈值区域自动选择一个正点提示；
- 调用 nnInteractive 做一次点提示分割；
- 输出 mask NIfTI、叠加预览 PNG 和 `probe_summary.json`。

输出目录：

```text
outputs/nninteractive_probe/
```

该目录属于运行产物，不提交到 git。

如果只想检查权重下载：

```powershell
D:/python/anaconda/envs/fracmed/python.exe scripts/probe_nninteractive.py --download-only
```

如果只想检查 DICOM 读取、NIfTI 导出和自动选点，不运行模型推理：

```powershell
D:/python/anaconda/envs/fracmed/python.exe scripts/probe_nninteractive.py --case 1 --no-inference
```

注意：nnInteractive 是交互式分割模型，不是骨折检测模型。当前阶段只把它作为“疑似区域/骨结构交互辅助”方向验证，不直接输出骨折诊断结论。

### 7.1 当前探针结果

病例 1 已跑通完整链路：

```text
DICOM -> NIfTI -> 自动正点提示 -> nnInteractive 推理 -> mask NIfTI -> PNG 预览
```

结果摘要：

- 输入体数据 shape：`520 x 644 x 644`
- spacing：`0.25 x 0.25 x 0.25 mm`
- 自动正点：`z=260, y=322, x=322`
- 总耗时：约 `36s`
- 模型推理阶段：约 `4s`
- mask voxel 数：`1439418`

输出文件：

```text
outputs/nninteractive_probe/case_1_cuda/case_1_nninteractive_mask.nii.gz
outputs/nninteractive_probe/case_1_cuda/case_1_mask_overlay.png
outputs/nninteractive_probe/case_1_cuda/probe_summary.json
```

当前观察：nnInteractive 能根据单个正点生成局部骨结构 mask，适合作为交互式分割/标注辅助方向继续接入页面。

### 7.2 License 注意

当前加载的官方模型权重报告 license 为：

```text
CC BY-NC-SA 4.0
```

因此在 demo、科研验证、内部技术评估之外，如果进入医院正式部署、商业交付或对外产品化阶段，需要重新核对授权条件。
